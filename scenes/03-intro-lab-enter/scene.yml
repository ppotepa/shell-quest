# yaml-language-server: $schema=../../schemas/scenes.yaml
id: 03.intro.lab-enter
title: 03 Intro Lab Enter
cutscene: true
bg: black
layers:
  - ref: main
stages:
  on_enter:
    steps:
      - { effects: [{ name: fade-in, duration: 220, params: { easing: ease-out } }] }
      - { pause: 1600ms }
      - { effects: [{ name: fade-out, duration: 260, params: { easing: ease-in } }] }
  on_idle:
    trigger: timeout
    steps: [{ pause: 1ms }]
  on_leave:
    steps: [{ pause: 80ms }]
next: 04.intro.difficulty-select
