$schema: https://json-schema.org/draft/2020-12/schema
$id: https://shell-quest.local/mods/shell-quest/schemas/effects.yaml
title: schema shell-quest effects
type: array
items:
  oneOf:
  - type: object
    properties:
      use:
        type: string
        minLength: 1
      preset:
        type: string
        minLength: 1
      ref:
        type: string
        minLength: 1
      overrides:
        type: object
        default: {}
        additionalProperties: true
    additionalProperties: true
    anyOf:
    - required:
      - use
    - required:
      - preset
    - required:
      - ref
    title: effect preset alias
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: brighten
        params:
          type: object
          additionalProperties: false
          properties:
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: brighten effect variant
    - description: brighten overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: clear-to-colour
        params:
          type: object
          additionalProperties: false
          properties:
            colour:
              type: string
              default: black
              description: 'Target colour (name or #rrggbb).'
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: clear-to-colour effect variant
    - description: clear-to-colour overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: crt-on
        params:
          type: object
          additionalProperties: false
          properties:
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: crt-on effect variant
    - description: crt-on overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: fade-in
        params:
          type: object
          additionalProperties: false
          properties:
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: fade-in effect variant
    - description: fade-in overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: fade-out
        params:
          type: object
          additionalProperties: false
          properties:
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: fade-out effect variant
    - description: fade-out overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: fade-to-black
        params:
          type: object
          additionalProperties: false
          properties:
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: fade-to-black effect variant
    - description: fade-to-black overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: lightning-ambient
        params:
          type: object
          additionalProperties: false
          properties:
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            strikes:
              type: number
              minimum: 1.0
              maximum: 10.0
              multipleOf: 1.0
              description: Number of primary arcs / branches.
            thickness:
              type: number
              minimum: 0.10000000149011612
              maximum: 3.0
              multipleOf: 0.10000000149011612
              description: Branch / bolt thickness multiplier.
            speed:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.10000000149011612
              description: Animation speed multiplier.
            glow:
              type: boolean
              default: true
              description: Draw halo glow around the bolt.
            orientation:
              type: string
              enum:
              - horizontal
              - vertical
              default: horizontal
              description: Directional axis for the effect.
      title: lightning-ambient effect variant
    - description: lightning-ambient overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: lightning-fbm
        params:
          type: object
          additionalProperties: false
          properties:
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            octave_count:
              type: number
              minimum: 1.0
              maximum: 8.0
              multipleOf: 1.0
              description: FBM complexity octaves.
            speed:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.10000000149011612
              description: Animation speed multiplier.
            orientation:
              type: string
              enum:
              - horizontal
              - vertical
              default: horizontal
              description: Directional axis for the effect.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: lightning-fbm effect variant
    - description: lightning-fbm overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: lightning-flash
        params:
          type: object
          additionalProperties: false
          properties:
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            coverage:
              type: string
              enum:
              - full-screen
              - region
              default: full-screen
              description: Scope hint for the flash.
            orientation:
              type: string
              enum:
              - horizontal
              - vertical
              default: horizontal
              description: Directional axis for the effect.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: lightning-flash effect variant
    - description: lightning-flash overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: lightning-growth
        params:
          type: object
          additionalProperties: false
          properties:
            strikes:
              type: number
              minimum: 1.0
              maximum: 10.0
              multipleOf: 1.0
              description: Number of primary arcs / branches.
            thickness:
              type: number
              minimum: 0.10000000149011612
              maximum: 3.0
              multipleOf: 0.10000000149011612
              description: Branch / bolt thickness multiplier.
            speed:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.10000000149011612
              description: Animation speed multiplier.
            glow:
              type: boolean
              default: true
              description: Draw halo glow around the bolt.
            orientation:
              type: string
              enum:
              - horizontal
              - vertical
              default: horizontal
              description: Directional axis for the effect.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: lightning-growth effect variant
    - description: lightning-growth overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: neon-edge-glow
        params:
          type: object
          additionalProperties: false
          properties:
            colour:
              type: string
              default: '#00ccff'
              description: 'Neon glow colour (name or #rrggbb).'
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            alpha:
              type: number
              minimum: 0.0
              maximum: 1.0
              multipleOf: 0.05000000074505806
              description: Spillover glow opacity.
            speed:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.10000000149011612
              description: Animation speed multiplier.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: neon-edge-glow effect variant
    - description: neon-edge-glow overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: scanlines
        params:
          type: object
          additionalProperties: false
          properties:
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
      title: scanlines effect variant
    - description: scanlines overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: shine
        params:
          type: object
          additionalProperties: false
          properties:
            angle:
              type: number
              minimum: 0.0
              maximum: 90.0
              multipleOf: 2.0
              description: Beam angle in degrees (0=vertical sweep).
            width:
              type: number
              minimum: 1.0
              maximum: 12.0
              multipleOf: 0.5
              description: Gaussian beam half-width in cells.
            falloff:
              type: number
              minimum: 0.0
              maximum: 5.0
              multipleOf: 0.20000000298023224
              description: Edge sharpness exponent (>1 = sharper).
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            speed:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.10000000149011612
              description: Animation speed multiplier.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: shine effect variant
    - description: shine overlay from shell-quest generated metadata
  - allOf:
    - $ref: ../../../schemas/effect-file.schema.yaml#/items
    - properties:
        name:
          const: whiteout
        params:
          type: object
          additionalProperties: false
          properties:
            intensity:
              type: number
              minimum: 0.0
              maximum: 2.0
              multipleOf: 0.05000000074505806
              description: Overall strength multiplier.
            easing:
              type: string
              enum:
              - linear
              - ease-in
              - ease-out
              - ease-in-out
              default: linear
              description: Progress curve applied to the effect.
      title: whiteout effect variant
    - description: whiteout overlay from shell-quest generated metadata
